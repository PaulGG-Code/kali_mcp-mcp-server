runtime: "container"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

startCommand:
  type: "http"
  timeout: 120000
  pingInterval: 5000
  pingTimeout: 30000

env:
  MCP_TRANSPORT: "http"

